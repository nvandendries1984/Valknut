<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <main>
        <div class="container py-4">
            <div class="mb-4">
                <h1 class="display-5 fw-bold">
                    <i class="fas fa-tachometer-alt me-2"></i>
                    Dashboard
                </h1>
                <p class="lead">Welkom terug, <strong><%= user.username %></strong>!</p>
            </div>

            <div class="row g-4 mb-4">
                <div class="col-md-6">
                    <div class="card bg-dark text-white border-secondary h-100">
                        <div class="card-body d-flex align-items-center">
                            <i class="fas fa-server fa-3x me-3 text-primary"></i>
                            <div>
                                <h3 class="mb-0"><%= allGuilds %></h3>
                                <p class="mb-0 text-muted">Jouw Servers</p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="card bg-dark text-white border-secondary h-100">
                        <div class="card-body d-flex align-items-center">
                            <i class="fas fa-robot fa-3x me-3 text-primary"></i>
                            <div>
                                <h3 class="mb-0"><%= mutualGuilds %></h3>
                                <p class="mb-0 text-muted">Met Bot</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <% if (guilds.length === 0) { %>
                <div class="card bg-dark text-white border-secondary text-center py-5">
                    <div class="card-body">
                        <i class="fas fa-inbox fa-5x mb-4 text-muted"></i>
                        <h2>Geen servers gevonden</h2>
                        <p class="text-muted mb-4">De bot is nog niet in een van jouw servers, of je hebt geen admin rechten.</p>
                        <a href="https://discord.com/api/oauth2/authorize?client_id=<%= process.env.CLIENT_ID %>&permissions=8&scope=bot%20applications.commands"
                           class="btn btn-primary btn-lg" target="_blank">
                            <i class="fas fa-plus me-2"></i>
                            Bot toevoegen aan server
                        </a>
                    </div>
                </div>
            <% } else { %>
                <div class="row g-4">
                    <% guilds.forEach(guild => { %>
                        <div class="col-md-6 col-lg-4">
                            <div class="card bg-dark text-white border-secondary h-100">
                                <div class="card-body">
                                    <div class="d-flex align-items-center mb-3">
                                        <% if (guild.icon) { %>
                                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png"
                                                 alt="<%= guild.name %>" class="rounded me-3" width="50" height="50">
                                        <% } else { %>
                                            <div class="bg-secondary rounded d-flex align-items-center justify-content-center me-3"
                                                 style="width: 50px; height: 50px; font-size: 1.5rem;">
                                                <%= guild.name.charAt(0) %>
                                            </div>
                                        <% } %>
                                        <h5 class="mb-0"><%= guild.name %></h5>
                                    </div>

                                    <div class="mb-3">
                                        <div class="d-flex align-items-center mb-2">
                                            <i class="fas fa-users me-2 text-primary"></i>
                                            <span><%= guild.memberCount %> leden</span>
                                        </div>
                                        <div class="d-flex align-items-center mb-2">
                                            <i class="fas fa-user-check me-2 text-primary"></i>
                                            <span><%= guild.registeredUsers %> geregistreerd</span>
                                        </div>
                                        <div class="d-flex align-items-center">
                                            <i class="<%= guild.logChannelId ? 'fas fa-check-circle text-success' : 'fas fa-exclamation-triangle text-warning' %> me-2"></i>
                                            <span><%= guild.logChannelId ? 'Log kanaal actief' : 'Geen log kanaal' %></span>
                                        </div>
                                    </div>

                                    <a href="/dashboard/guild/<%= guild.id %>" class="btn btn-primary w-100">
                                        <i class="fas fa-cog me-2"></i>
                                        Beheren
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } %>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
