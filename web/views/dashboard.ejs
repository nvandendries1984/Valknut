<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <main>
<div class="container dashboard">
    <div class="page-header">
        <h1>
            <i class="fas fa-tachometer-alt"></i>
            Dashboard
        </h1>
        <p>Welkom terug, <strong><%= user.username %></strong>!</p>
    </div>

    <div class="stats-overview">
        <div class="stat-card">
            <i class="fas fa-server"></i>
            <div class="stat-info">
                <h3><%= allGuilds %></h3>
                <p>Jouw Servers</p>
            </div>
        </div>

        <div class="stat-card">
            <i class="fas fa-robot"></i>
            <div class="stat-info">
                <h3><%= mutualGuilds %></h3>
                <p>Met Bot</p>
            </div>
        </div>
    </div>

    <% if (guilds.length === 0) { %>
        <div class="empty-state">
            <i class="fas fa-inbox"></i>
            <h2>Geen servers gevonden</h2>
            <p>De bot is nog niet in een van jouw servers, of je hebt geen admin rechten.</p>
            <a href="https://discord.com/api/oauth2/authorize?client_id=<%= process.env.CLIENT_ID %>&permissions=8&scope=bot%20applications.commands"
               class="btn btn-primary" target="_blank">
                <i class="fas fa-plus"></i>
                Bot toevoegen aan server
            </a>
        </div>
    <% } else { %>
        <div class="guilds-grid">
            <% guilds.forEach(guild => { %>
                <div class="guild-card">
                    <div class="guild-header">
                        <% if (guild.icon) { %>
                            <img src="https://cdn.discordapp.com/icons/<%= guild.id %>/<%= guild.icon %>.png"
                                 alt="<%= guild.name %>" class="guild-icon">
                        <% } else { %>
                            <div class="guild-icon-placeholder">
                                <%= guild.name.charAt(0) %>
                            </div>
                        <% } %>
                        <h3><%= guild.name %></h3>
                    </div>

                    <div class="guild-stats">
                        <div class="stat">
                            <i class="fas fa-users"></i>
                            <span><%= guild.memberCount %> leden</span>
                        </div>
                        <div class="stat">
                            <i class="fas fa-user-check"></i>
                            <span><%= guild.registeredUsers %> geregistreerd</span>
                        </div>
                        <div class="stat">
                            <i class="<%= guild.logChannelId ? 'fas fa-check-circle text-success' : 'fas fa-exclamation-triangle text-warning' %>"></i>
                            <span><%= guild.logChannelId ? 'Log kanaal actief' : 'Geen log kanaal' %></span>
                        </div>
                    </div>

                    <a href="/dashboard/guild/<%= guild.id %>" class="btn btn-primary btn-block">
                        <i class="fas fa-cog"></i>
                        Beheren
                    </a>
                </div>
            <% }); %>
        </div>
    <% } %>
</div>
    </main>

    <%- include('partials/footer') %>

    <script src="/js/main.js"></script>
</body>
</html>
