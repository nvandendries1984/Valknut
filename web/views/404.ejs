<!DOCTYPE html>
<html lang="<%= typeof currentLang !== 'undefined' ? currentLang : 'en' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= typeof botName !== 'undefined' ? botName : 'Valknut' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <%- include('partials/navbar', { user: typeof user !== 'undefined' ? user : null, botName: typeof botName !== 'undefined' ? botName : 'Valknut' }) %>

    <main>
<div class="container error-page">
    <div class="error-content">
        <i class="fas fa-exclamation-triangle"></i>
        <h1>404</h1>
        <h2><%= typeof __ !== 'undefined' ? __('errors.404') : 'Page Not Found' %></h2>
        <p><%= typeof __ !== 'undefined' ? __('errors.404Desc') : 'The page you are looking for does not exist.' %></p>
        <a href="/" class="btn btn-primary">
            <i class="fas fa-home"></i>
            <%= typeof __ !== 'undefined' ? __('errors.goHome') : 'Go Home' %>
        </a>
    </div>
</div>
    </main>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
