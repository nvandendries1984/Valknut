<nav class="navbar navbar-expand-lg navbar-dark bg-dark border-bottom border-secondary">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="/">
            <% if (typeof botUser !== 'undefined' && botUser && botUser.avatar) { %>
                <img src="https://cdn.discordapp.com/avatars/<%= botUser.id %>/<%= botUser.avatar %>.png?size=64"
                     alt="<%= botUser.username %>" class="rounded-circle me-2" width="32" height="32">
            <% } else { %>
                <i class="fas fa-shield-alt me-2"></i>
            <% } %>
            Valknut
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <!-- Status Link (Always Visible) -->
                <li class="nav-item">
                    <a class="nav-link" href="https://status.nvandendries.nl/status/valknut" target="_blank">
                        <i class="fas fa-heartbeat me-1"></i>
                        Status
                    </a>
                </li>
                <% if (typeof user !== 'undefined' && user && (typeof showUserNav === 'undefined' || showUserNav !== false)) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard">
                            <i class="fas fa-tachometer-alt me-1"></i>
                            <%= (typeof __ !== 'undefined' ? __('nav.dashboard') : 'Dashboard') %>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard/tutorial">
                            <i class="fas fa-graduation-cap me-1"></i>
                            Tutorial
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/dashboard/help">
                            <i class="fas fa-question-circle me-1"></i>
                            <%= (typeof __ !== 'undefined' ? __('nav.help') : 'Help') %>
                        </a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png"
                                 alt="Avatar" class="rounded-circle me-2" width="32" height="32">
                            <%= user.username %>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <% if (process.env.OWNER_ID === user.id) { %>
                                <li><a class="dropdown-item" href="/admin/users">
                                    <i class="fas fa-user-shield me-2"></i><%= (typeof __ !== 'undefined' ? __('nav.manageUsers') : 'Manage Users') %>
                                </a></li>
                                <li><a class="dropdown-item" href="/admin/backups">
                                    <i class="fas fa-database me-2"></i>Backup Management
                                </a></li>
                                <li><hr class="dropdown-divider"></li>
                            <% } %>
                            <li><a class="dropdown-item" href="/auth/2fa/status">
                                <i class="fas fa-shield-alt me-2"></i><%= (typeof __ !== 'undefined' ? __('nav.twoFaSettings') : '2FA Settings') %>
                            </a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/auth/logout">
                                <i class="fas fa-sign-out-alt me-2"></i><%= (typeof __ !== 'undefined' ? __('nav.logout') : 'Logout') %>
                            </a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a href="/auth/login" class="btn btn-primary">
                            <i class="fab fa-discord me-1"></i>
                            <%= __('nav.loginWithDiscord') %>
                        </a>
                    </li>
                <% } %>


            </ul>
        </div>
    </div>
</nav>
